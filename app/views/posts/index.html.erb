<p id="notice"><%= notice %></p>

<div class="container">
  <%= render 'filter' %>
</div>

<div class="form-group container">
  <%= form_tag({:controller=>"posts", :action=>"scrape"}, method: :post) do %>
    <div class="form-group">
      <%= text_field_tag :location, nil, class: 'form-control input-lg text-center center-block', placeholder: 'Location', style:"width: 50%" %>
    </div>
    <div class="form-group">
      <%= submit_tag 'Search Donut', name: nil, class: 'btn btn-success search-button'%>
    </div>
  <% end %>
</div>


<div class="center-block">
  <% if @posts.empty? %>
    <h3>No result found</h3>
  <% else %>
  <% @posts.each do |post| %>
  <div>
    <a data-toggle="collapse" class="btn btn-lg custom-btn custom" data-parent="#accordion" href='<%= "#collapse_#{post.id}" %>' aria-expanded="false">
      <%= post.name %>
    </a>
    <div id='<%= "collapse_#{post.id}" %>' class="collapse">
      <div class="donut-content">
        some text
        Bacon ipsum dolor amet sirloin ground round turkey, chuck shank flank bresaola meatball t-bone venison doner. Venison pig biltong turducken, beef ribs rump chuck beef tail ground round salami. Ribeye pork boudin tenderloin rump, bacon prosciutto porchetta burgdoggen ball tip pork loin picanha flank meatball hamburger. Brisket hamburger swine spare ribs, sausage capicola short ribs ball tip alcatra. Cupim pancetta jerky pork chop turkey.
      </div>
    </div>
  <% end %>

  <!--
</div>
    <table class="table table-condensed table-nonfluid" id="donut-attributes">
      <tbody class="table-hover">
        <% @posts.each do |post| %>

          <tr address="<%= post.location %>" id="address-location">
            <td rowspan=5>
              <% if post.image.url.nil? %>
                <%= image_tag 'donut_image.png', class: 'thumbnail', style:'width:100px;height:100px;' %>
              <% else %>
                <%= image_tag post.image.url, class: 'thumbnail', style:"width:100px;height:100px;" %>
              <% end %>
            </td>
          </tr>

          <tr>
            <td class="center" style="vertical-align: middle"><%= link_to post.name, post %></td>
          </tr>

          <tr>
            <td class="center" style="vertical-align: middle"><%= number_to_phone(post.phone, area_code: true) %></td>
          </tr>

          <tr>
            <td class="center" style="vertical-align: middle"><%= post.rating %></td>
          </tr>

          <tr>
            <td class="center" style="vertical-align: middle" id="address" ><%= post.location %></td>
          </tr>

        <% end %>
      </tbody>
    <% end %>
  </table>
-->

  <%= will_paginate @posts, renderer: BootstrapPagination::Rails  %>
<!--
<%= image_tag 'city.png', style:'position:fixed;bottom:0px;' %>
-->
</div>
